"use client";
/**
 * page.tsx (Landing Page)
 *
 * Displays only the HomeMemorialCard, centered on the page.
 */
import React from 'react';
import HomeMemorialCard from '../components/memorial/HomeMemorialCard';

const HomePage: React.FC = () => {
  return <HomeMemorialCard />;
};

export default HomePage; 